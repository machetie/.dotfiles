function bls --wraps='betterlockscreen -u /usr/share/backgrounds/arcolinux/' --description 'alias bls betterlockscreen -u /usr/share/backgrounds/arcolinux/'
  betterlockscreen -u /usr/share/backgrounds/arcolinux/ $argv; 
end
